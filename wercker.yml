box: wercker/golang
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - script:
        name: setup golang
        code: |
          mkdir $HOME/mygo
          export GOPATH=$HOME/mygo
          export PATH=$PATH:$GOPATH/bin
          mkdir $GOPATH/bin
          mkdir $GOPATH/pkg
          if test "${GIT_SSH_PROJECT+set}" == set; then mkdir -p $GOPATH/src/$GIT_SSH_DOMAIN/$GIT_SSH_OWNER/$GIT_SSH_PROJECT; else ln -s $WERCKER_SOURCE_DIR $GOPATH/src; fi
          if test "${GIT_SSH_PROJECT+set}" == set; then cp -R $WERCKER_SOURCE_DIR/* $GOPATH/src/$GIT_SSH_DOMAIN/$GIT_SSH_OWNER/$GIT_SSH_PROJECT;  fi
          if test "${GIT_SSH_PROJECT+set}" == set; then export WERCKER_SOURCE_DIR=$GOPATH/src/$GIT_SSH_DOMAIN/$GIT_SSH_OWNER/$GIT_SSH_PROJECT; fi
    - script:
        name: go get
        code: |
          go version
          go get ./...
    - script: 
        name: go build
        code: |
          go build .
    - script:
        name: go test
        code: |
          go test ./...
